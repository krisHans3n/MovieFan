<%= form_with(model: movieswatched, local: true) do |form| %>
  <% if movieswatched.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movieswatched.errors.count, "error") %> prohibited this movieswatched from being saved:</h2>

      <ul>
        <% movieswatched.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :movies %>
    <%= form.text_field :movies %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
